import ContactForm from "@/components/forms/ContactForm";

export const metadata = {
  title: "Contact",
  description: "Tell us about the property or financing scenario you are considering — investor, broker or general inquiries.",
};

export default function ContactPage() {
  return <ContactForm />;
}
